{
  "name": "battlecmo",
  "version": "0.1.0",
  "description": "Overwatch Directive — deterministic multi-domain strike/rescue C2 simulation core (headless)",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "dev": "vite",
    "build": "vite build",
    "build:pages": "vite build --base=/Battlecmo/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "playwright": "^1.61.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.21",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
